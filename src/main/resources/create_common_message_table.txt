create table common_message_data(
`cId` bigint(20)  not null auto_increment PRIMARY KEY comment '主键',
`cTitle` varchar(255) not null DEFAULT '' comment '消息标题',
`cContent` varchar(255) comment '消息内容',
`cPublisher` varchar(255) comment '消息发布人',
`cPublishTime` datetime comment '创建时间'
);